{{- if .Values.profiles }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "chaosmonkey.fullname" . }}-profiles
  labels:
    {{- include "chaosmonkey.labels" . | nindent 4 }}
data:
  profiles.yaml: |
    {{- toYaml .Values.profiles | nindent 4 }}
{{- end }}
